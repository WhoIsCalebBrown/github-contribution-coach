# Contributing

Useful contributions improve the quality of the opportunities the coach finds,
make its accounting more accurate, or support another forge without automating
social activity.

The project will not accept features that manufacture commits, file low-value
issues, submit generated pull requests, or post automated reviews.

Run the test suite before opening a pull request:

```bash
python -m unittest discover -s tests -v
```
